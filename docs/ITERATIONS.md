# 持续体验迭代记录

这份文件记录首版功能完成后的每一轮产品迭代。每轮遵循同一个闭环：

1. **体验功能**：以真实任务走完关键路径，记录卡顿、犹豫、错误与无效操作。
2. **思考优化**：区分功能缺失、信息架构、交互反馈、视觉层级、性能和可靠性问题。
3. **写迭代产品稿**：明确用户场景、问题证据、设计决策、验收标准与不做事项。
4. **开发**：小步实现，数据迁移向后兼容，不牺牲本地隐私边界。
5. **测试**：静态检查、构建、接口、持久化、异常路径、键盘和响应式回归。

完成第五步后立即回到第一步。只有通过验收的改动才进入 Git 检查点。

## 迭代 0 · 完整首版基线

### 用户场景

- 用户希望用一套私人、本地运行的工具管理完整求职过程，并保存可复盘的面经。
- 用户希望从英文文章和播客的真实语境中学习词汇，英文解释优先，必要时再看中文。
- AI 只在主动触发时参与，密钥由服务器侧环境变量管理；所有核心功能在 AI 不可用时仍可使用。

### 设计决策

- 两款应用拥有完全独立的视觉系统和数据库，入口页保持中性。
- SQLite 运行在专用 Worker，并使用浏览器 OPFS；大文件不写进 SQLite。
- LinkedIn 与 BOSS 采用原链接、分享文本、CSV 和浏览器采集器，不模拟登录或绕过平台限制。
- 播客优先接受带字幕内容与本地字幕导入；自动转录作为独立、可选的兼容端点。

### 首版验收线

- 职迹：职位、阶段、任务、日历、面试/面经、人脉、材料、数据与设置主路径完整可用。
- 拾词：文章/RSS/播客/字幕导入、上下文选择、AI 解释、词库、间隔复习、统计和设置完整可用。
- 刷新与重启后数据仍在；两款应用数据互不影响；可导出并恢复备份。
- API 密钥不进入浏览器数据库、导出文件、日志或 Git。
- lint、类型检查、生产构建和关键自动化测试全部通过。

### 完成状态

进行中。首版达到验收线后开始记录迭代 1。

---

## 迭代 1 · 职迹首次启动可靠性

### 体验证据

- 任务：从套件首页点击“职迹”，等待首次本地数据库初始化。
- 观察：页面进入阻断状态；SQLite Worker 被 vinext 的客户端模块运行时解析为 `file://` 地址，浏览器因来源不同拒绝启动。
- 影响：静态构建和服务端首屏均可通过，但真实浏览器无法打开任何求职数据，是所有后续体验的最高优先级阻断。

### 迭代产品稿

- 目标：无论开发构建还是生产构建，数据库 Worker 都必须从当前 `http://localhost:3000` 来源加载；首次打开可完成建表和示例数据写入。
- 方案：保留 Vite 对静态 Worker URL 的打包识别，并在 vinext 暴露文件协议 URL 时，使用其已生成的 pathname/search 在浏览器当前来源上重建 URL。
- 验收标准：`crossOriginIsolated === true`；职迹首次打开进入主界面；Worker URL 为同源 HTTP；刷新后数据仍存在；控制台无 Worker/SQLite 错误；生产构建继续通过。
- 本轮不做：不在可靠性未通过前调整看板或视觉细节。

### 开发与验证

- 改动：在保留 Vite Worker 静态打包识别的同时，将 vinext 暴露的 `file://` Worker 地址重建为当前页面同源 URL；SQLite Worker 仅在参数非空时执行 `bind()`，避免无占位符语句在首次建表时中断。
- 测试：真实浏览器完成首次建表与种子写入；首页显示 6 个活跃机会、4 个待办和 1 场面试；完成一项待办后刷新，剩余 3 项状态保持；浏览器控制台无错误或警告。SQLite Worker 成功启用依赖 `SharedArrayBuffer`，结合运行时 COOP/COEP 响应头验证隔离前提已满足。
- 结果：通过。开发环境中的 Worker 同源启动、首次初始化与刷新持久化主路径恢复。
- 下一轮候选：完成事项缺少清晰的回看/撤销入口；主界面信息密度、首要动作、字号与对比度需要视觉专项审计。

---

## 迭代 2 · 不给求职者打分

### 体验证据

- 任务：打开职迹首页和分析页，判断自己今天应该做什么。
- 观察：首页把“有效回复率”作为四个核心数字之一；分析页继续展示“面试转化”“Offer 率”“待办完成率”和渠道推进率；侧栏还用“本周投递 1 / 8”呈现固定目标。
- 用户反馈：当暂时收不到 Offer 时，这类结果率不是帮助，而是反复提醒失落、制造压力。功能越多、数字越多，并不等于体验越好。
- 影响：产品把不可完全控制的招聘结果包装成个人成绩，让原本用于减轻负担的私人工作台增加了情绪成本。

### 迭代产品稿

- 目标：职迹只帮助用户看清可控的下一步、等待状态与可沉淀的经验，不把 Offer、面试或任务完成情况变成评分。
- 方案：首页用“等待回应”替代任何比率；侧栏删除固定投递目标，改为不设目标的近期记录；“求职分析”改为“过程回顾”，默认只展示正在推进、等待回应、已安排下一步和已写面经的数量。来源模块只说明记录分布，不评价渠道优劣；结束阶段不进入默认图表。
- 情绪成本审查：每个统计必须回答“它会改变哪个可控决定”；无法回答，或主要作用是比较、催促、评判时，不展示。空状态和提醒避免“落后”“失败”“应该更多”等措辞。
- 验收标准：界面中不再出现 Offer 率、面试转化率、回复率、任务完成率或固定周投递目标；保留阶段和原始记录；分析页仍能帮助发现没有下一步的机会；所有文案在零 Offer、零面试时依然中性、尊重。
- 本轮不做：不删除任何历史数据，不隐藏用户主动打开的具体职位结果，不把温和体验等同于回避事实。

### 开发与验证

- 改动：移除首页有效回复率、分析页 Offer/面试/完成率、渠道推进率和侧栏固定周投递目标；新增“等待回应”、不设目标的近期记录，以及只呈现可控状态的“过程回顾”。默认阶段图不再展示已拒绝、已接受和已退出等结束状态。
- 测试：真实浏览器逐项检查首页与过程回顾；确认侧栏明确显示“不设目标，也不给你打分”，首页只呈现等待数量，过程回顾只呈现当前行动、下一步和面经记录；代码搜索确认旧比率和周目标文案已清除；浏览器无运行时错误。Career scoped lint 与全量类型检查通过。
- 结果：通过。Offer 等具体事实仍保留在职位记录和时间线中，但不会再被聚合成对用户的成绩。
- 下一轮候选：已完成待办的回看与恢复；Apple 风格视觉专项。

---

## 迭代 3 · 完成之后仍然可找回

### 体验证据

- 任务：在“今天与接下来”中勾选一条待办，然后刷新并尝试确认刚才的操作。
- 观察：任务会立即从首页和待办清单消失；刷新后状态正确保留，但界面没有撤销提示，也没有已完成列表可以核对或恢复。
- 影响：高频的一次点击带来不可见的状态变化，用户容易担心点错；“消失”也削弱了工具替用户保存过程记忆的承诺。

### 迭代产品稿

- 目标：完成动作轻快，但永远可确认、可撤销、可恢复；承认做过的事情，不把它们换算成完成率。
- 方案：每次切换待办状态后显示短时撤销提示；议程页增加默认收起的“已完成”区域，展示任务与关联职位，并允许一键恢复。
- 验收标准：首页勾选后出现“待办已完成 / 撤销”；点击撤销后任务原位恢复且刷新后仍为未完成；不撤销时可在议程的已完成区找到；恢复后回到待办清单；键盘与读屏能识别展开状态和操作名称。
- 本轮不做：不引入积分、连续天数、庆祝动效或完成率；不删除已完成记录。

### 开发与验证

- 改动：待办状态切换后显示具名的短时撤销按钮；议程新增默认收起的已完成区域，可查看关联职位并恢复；无待办时使用中性空状态。
- 测试：真实浏览器完成“未完成→完成→即时撤销→再次完成→刷新→已完成区回看→恢复”的双向链路；撤销提示和展开状态具备可访问名称；最终把测试记录恢复为原始已完成状态。刷新后仍显示 3 个未完成与 1 个已完成，浏览器无错误；Career scoped lint 与全量类型检查通过。
- 结果：通过。完成不再等于消失，也没有引入完成率或奖励机制。
- 下一轮候选：Apple 风格视觉专项。

---

## 迭代 4 · 职迹 Apple 风格视觉专项

### 体验证据

- 任务：在 390px 移动视口与桌面结构中扫描今日、议程和过程回顾，判断是否能从容地看清首要信息并可靠操作。
- 观察：绿色/米白、圆角和留白已有克制基础，但 158 个显式字号中 129 个不超过 11px；弱文字对比度约 2.86:1；按钮、勾选框和图标操作普遍偏小。巨大标题与微型正文之间缺少稳定中层，卡片、边框和徽章层层嵌套，整体更像微缩 BI 面板。
- 影响：页面第一眼有精致感，但需要费力辨认；移动端输入可能触发 iOS 自动放大，键盘焦点也不够清楚。“高级”停留在装饰，而没有落实为清晰、安静和可信赖。

### 迭代产品稿

- 目标：建立接近 Apple 产品界面的从容层级——内容先于容器，清晰先于密度，动效服务于方位感；保持职迹自己的深绿身份，不复制 Apple 商标或具体界面。
- 视觉语言：使用系统字体与五级字阶；正文不小于 13–14px、元数据不小于 12px、移动表单 16px；页面背景改为中性浅灰，白色表面只保留两级层次；统一 16–22px 圆角、发丝分隔线和低不透明度阴影。
- 交互语言：主要按钮和移动触控目标 44px；所有原生与自定义控件使用高对比焦点环；窄屏关闭侧栏不能进入 Tab 顺序；安全区和动态视口正确预留；Reduce Motion 下停止位移动画与旋转。
- 情绪成本审查：视觉强调只给“下一步”和正在编辑的对象，不用红色、徽章、进度条或动效催促投递数量。
- 验收标准：无 UI 文本小于 12px；正文/弱文字达到 4.5:1，焦点态达到 3:1；移动表单不缩放；390px 页面无非预期横向滚动；底栏按钮在安全区内仍有 44px；今日、议程和过程回顾截图能按标题→下一步→辅助信息自然扫描。
- 本轮分段：先完成字阶、对比度、触控与表面层级；随后单独处理弹层焦点模型、键盘看板和移动职位列表，避免一次重构引入不可定位的回归。

### 开发与验证

- 改动：重建深绿 + 中性浅灰的表面系统与系统字体字阶；所有显式 UI 字号提升至至少 12px，正文和关键操作为 13–16px；增强弱文字与焦点环对比度；按钮、导航、搜索、筛选和移动底栏扩大至 40–56px；卡片统一更从容的圆角、留白和轻阴影。首页把“最重要的一步”移到统计卡之前；移动端修复动态视口、安全区、侧栏关闭后仍可聚焦、表单自动放大、任务列挤压和 Reduce Motion。
- 测试：390px 真实页面截图复核今日与议程，1440×900 复核桌面首屏，800px 验证图标导航仍有中文可访问名称，测试后恢复用户原视口；代码扫描确认无小于 12px 的显式字号；`#68716c` 弱文字在白底/页面底分别为 5.04:1/4.63:1，`#087a55` 焦点色分别为 5.35:1/4.91:1；浏览器无错误或警告。Career scoped lint 与全量类型检查通过。
- 结果：本阶段通过。页面扫描顺序已从“问候→统计成绩”变为“问候→下一步→辅助统计”，视觉更安静且可读。
- 下一轮候选：弹层焦点圈定与移动端职位卡片。

---

## 迭代 5 · 弹层像系统界面一样可靠

### 体验证据

- 任务：仅用键盘打开“记录职位”、职位抽屉、面经抽屉和全局搜索，再尝试 Tab、Shift+Tab、Escape 与关闭后继续操作。
- 观察：弹层有视觉遮罩，但缺少可访问名称、初始焦点、焦点圈定、背景禁用和触发器焦点归还；Escape 只关闭部分状态，职位/面经抽屉不响应。搜索面板还展示了未实现的方向键提示。
- 影响：视觉上像系统弹层，行为上却不可信；键盘与读屏用户可能“掉到”背景页面，关闭后也不知道焦点去了哪里。

### 迭代产品稿

- 目标：Modal、Drawer 与搜索面板统一遵循系统弹层心智模型：有名字、打开即进入、焦点不逃逸、Escape 可关闭、关闭回原处。
- 方案：建立共享弹层可访问性钩子；记录触发器，给背景兄弟节点设置 inert，锁定页面滚动，聚焦第一个可编辑控件，圈定 Tab，并在卸载后恢复背景与焦点。每种弹层通过 `aria-labelledby` 或 `aria-label` 命名；删除搜索面板未实现的键盘承诺。
- 验收标准：真实浏览器打开各类弹层后 active focus 位于弹层内；连续 Tab/Shift+Tab 不进入背景；Escape 可关闭；关闭后焦点回触发器；DOM 中所有 dialog 均有名称，关闭按钮和遮罩有操作名称；嵌套 AI 预览不会错误解锁其后的职位抽屉。
- 本轮不做：不在本轮重做弹层内容结构，也不引入第三方组件库。

### 开发与验证

- 改动：新增共享弹层可访问性钩子，统一初始焦点、Tab/Shift+Tab 圈定、Escape、背景 inert、滚动锁定和焦点归还；Modal 使用标题/说明 ID 命名，Drawer 与全局搜索使用具体标签；补齐遮罩、关闭、阶段、面经问题输入和删除按钮名称；删除全局搜索未实现的方向键/回车提示。
- 测试：真实浏览器验证“记录职位”打开后公司输入自动聚焦，首尾 Tab 双向循环，Escape 关闭并回到原“记录职位”按钮；职位抽屉以公司/职位命名、阶段选择自动聚焦、Escape 回到“打开职位”；全局搜索具名、输入自动聚焦并正确归还。进一步从职位抽屉调用真实 DeepSeek 匹配分析，约 10.5 秒成功返回草稿；关闭 AI 预览后职位抽屉保持打开且焦点回到“匹配度分析”，再关闭抽屉背景才恢复。浏览器无错误或警告，Career scoped lint 与全量类型检查通过。
- 结果：通过。三类弹层与嵌套 AI 场景拥有一致、可预测的系统行为。
- 下一轮候选：移动职位摘要卡与键盘看板。

---

## 迭代 6 · 窄屏职位摘要与等价阶段操作

### 体验证据

- 任务：在约 390px 宽度打开“全部职位”，浏览职位并更改看板阶段；随后只用键盘完成同一任务。
- 观察：移动页面仍渲染最小宽度 920px 的桌面表格，首屏只看到职位和半截阶段，必须横向拖动才能知道下一步；看板只配置 PointerSensor，职位卡还是一个包含拖动按钮的 `role=button`，键盘没有等价的阶段切换入口。
- 影响：手机上最常见的快速查看变成二维找信息；“拖起来很顺”掩盖了没有鼠标时根本无法推进职位的问题。

### 迭代产品稿

- 目标：窄屏以一张卡回答“这是什么职位、在哪个阶段、下一步是什么”，无需横滚；所有用户都能用明确控件推进阶段。
- 方案：桌面保留信息密集表格，≤760px 切换为独立职位摘要卡；筛选控件具名。看板卡片拆成非嵌套的打开按钮、拖动柄和阶段选择器；拖动保留，阶段选择器作为始终可见的键盘/触控等价路径。
- 验收标准：390px 页面本身与职位列表无横向滚动；每张卡显示职位、公司、阶段、来源、地点和下一步；点击卡片打开详情。仅键盘可聚焦任意看板卡的阶段选择器并移动到所有可见阶段，移动后刷新仍保留；卡片不再出现交互控件嵌套。
- 本轮不做：不强迫移动看板改成纵向列表；看板横向空间仍可作为明确的二维工作区。

### 开发与验证

- 改动：≤760px 隐藏桌面表格并渲染独立职位摘要卡，完整呈现阶段、来源、地点和下一步；移动筛选改为两列网格并补齐名称/pressed 状态。看板职位卡拆分为语义清晰的 article、打开按钮与拖动柄，新增具名阶段选择器作为键盘/触控等价操作；看板说明同步告知两种操作方式。
- 测试：真实 390px 页面确认 DOM 不再渲染可见 table，每张职位卡只包含一个具名打开按钮且信息完整；移动截图无职位表横向裁切。看板 DOM 中拖动、打开与阶段选择是三个同级控件；使用 Anthropic 卡片阶段菜单从“收藏”移动到“准备中”，刷新后仍为“准备中”，随后移回“收藏”并再次刷新确认测试数据完全恢复。浏览器无错误或警告，Career scoped lint 与全量类型检查通过。
- 结果：通过。移动职位页成为单向扫描，阶段推进不再依赖拖拽或鼠标。
- 下一轮候选：面经完整保存与回看、职位导入真实任务。

---

## 迭代 7 · 面经是可回看的经验，不是一次性表单

### 体验证据

- 任务：打开一条已完成面经，在“复盘与下一步”加入临时标记并保存；关闭抽屉、刷新页面、重新进入面经档案核对；最后恢复原始内容并再次刷新确认。
- 观察：总结、原始速记、问题/回答与复盘能够写入本地 SQLite，刷新后仍完整存在；但面经卡片本身是一个 `role="button"`，内部又嵌套“打开面经”、AI 与会议链接，形成重复且不可靠的交互层级。即将进行/面经档案也只用颜色表达当前状态。
- 影响：核心数据承诺已经成立，但入口语义让键盘和读屏用户难以判断到底在操作卡片还是卡片里的按钮；这会削弱“经验一直找得到”的可信感。

### 迭代产品稿

- 目标：面经从记录到回看形成可靠闭环；整张卡保持清楚的阅读层级，但每个动作只有一个明确入口，不制造嵌套按钮或重复焦点。
- 方案：面经卡改为语义化 article；标题区提供独立且具名的“打开面经”按钮，AI 准备与会议链接作为同级动作。分段选择补充 `aria-pressed`，当前状态不只依赖视觉。保存后保留在抽屉内并给出明确反馈，方便继续核对。
- 验收标准：修改任一面经字段并保存，刷新后值不丢失；恢复原值后刷新仍正确；每张卡不存在交互角色嵌套，Tab 顺序只经过真实动作；分段按钮暴露当前状态；Escape 关闭抽屉并回到触发按钮；浏览器无错误。
- 本轮不做：不自动把 AI 输出覆盖进面经，也不把面经数量变成完成率或评分；AI 草稿的持久化将单独迭代，避免“看似已保存”的误导。

### 开发与验证

- 改动：面经卡从可点击的 `div role="button"` 改为语义化 article；打开、AI 准备与会议链接成为互不嵌套的同级动作，打开按钮包含具体公司与轮次名称；“即将进行 / 面经档案”补充 pressed 状态。
- 测试：真实浏览器把 Figma 面经复盘从原文改为带“【回归测试】”标记，保存、关闭、刷新并重新打开后确认标记仍在；随后恢复原文，再次保存、关闭、刷新、重新打开，确认数据完全恢复。DOM 检查即将进行卡为 ARTICLE、无 role、卡内 2 个真实动作且非法嵌套为 0；分段状态为 true/false；打开 Figma 面经后 dialog 有具体名称，Escape 关闭并把焦点归还到“打开 Figma · Hiring Manager 面经”。浏览器无错误，Career scoped lint 与全量类型检查通过。
- 结果：通过。面经从写入到回看已形成可靠闭环，入口不再靠嵌套交互模拟整卡点击。
- 下一轮候选：LinkedIn / BOSS直聘导入真实任务、删除无效设置与按钮。

---

## 迭代 8 · 界面上的每一个承诺都必须是真的

### 体验证据

- 任务：从头像进入设置、切换“保留 AI 原始内容”，再生成一份 AI 草稿并尝试保留；同时核对材料定制、联系人更多按钮和备份提示的实际落位。
- 观察：头像只有“个人设置”名称却不导航；AI 原文开关点击前后 DOM、状态和样式完全不变；“已连接”是硬编码；AI 的“接受为草稿”只关闭预览并提示已保留，没有写入 SQLite、表单、剪贴板或草稿表。材料定制没有材料选择，也没有读取附件正文，却声称会保存草稿；联系人更多按钮没有动作；SQLite 备份明确不含 OPFS 附件，却提示“完整备份”。
- 影响：这些入口不会直接损坏数据，却会让用户误以为重要资料已被保留、AI 已拿到完整材料或备份已覆盖原件。功能越多，信任反而越少。

### 迭代产品稿

- 目标：没有落位的能力不冒充“已保存”，没有实时检查的状态不冒充“已连接”，没有实现的控件不保留装饰性入口。
- 方案：AI 预览只提供真实的“复制结果”与“关闭预览”，复制成功后才反馈；职位“匹配度分析”改为不打分的“拆解职位要求”，仅整理 JD 事实与待确认问题；删除无法读取材料正文的 AI 定制入口。设置实时读取 `/api/health`，删除假开关和假拖动柄；头像真正进入设置，删除联系人无效更多按钮。备份文案明确只包含 SQLite 结构化数据与附件索引、不含附件原件。
- 过程准确性：待进行面试只认 `scheduled`；`canceled` 进入档案并明确显示已取消，不再出现在日程、下一步或“已有下一步”里。空白复盘不显示“待复盘”欠债状态。
- 验收标准：点击头像进入设置；AI 状态来自 health 接口并能区分检查中/已配置/未配置/失败；页面没有无事件的开关、拖动柄和联系人更多按钮；AI 预览没有“接受为草稿”及虚假保存提示，复制按钮实际写入剪贴板；职位拆解结果不含 overall score；导出/恢复提示不再说完整备份； canceled 面试只出现在档案。
- 本轮不做：不在没有明确目标表结构前建立泛化 AI 草稿库；不把简历附件正文悄悄发送给模型；联系人完整编辑将在独立迭代完成。

### 开发与验证

- 改动：头像现在进入设置；AI 区实时读取 `/api/health` 并显示实际模型/已配置/未配置/失败，假原文开关与假阶段拖动柄已删除。AI 预览改为“关闭预览 / 复制结果”，复制使用 Clipboard API 并带本地选择回退，按钮本身显示复制中/已复制/失败；删除无法读取附件正文的材料定制和无事件的联系人更多按钮，无附件材料入口改成明确的“新建带附件版本”。职位“匹配度分析”改成不评分的“职位要求拆解”，服务端契约不再包含 overall score；删除职位抽屉里名实不符的“AI 下一步”。联系人最近联系可留空，不再新增即虚构“今天联系”。备份提示明确不含附件原件。
- 过程修正：日程、今日下一步、看板与过程回顾只把 `scheduled` 视为待进行；档案收纳 completed/canceled，取消项明确标记“已取消”；没有复盘内容时不显示“待复盘”。同时修复移动面经底部保存栏落到视口外的问题。
- 测试：真实设置页确认头像可达、DeepSeek 状态为“已配置”、模型为 `deepseek-v4-flash`，假开关和阶段装饰性 SVG 均为 0；材料页不存在“开始定制/保存为草稿”，联系人卡只剩真实“创建待办”。真实调用 DeepSeek 生成 Arc 职位要求拆解，结果把所有未提供的个人证据标为 unknown，不含 `overall_score` 或 score 字段；点击复制后按钮变为“已复制”，预览关闭后职位抽屉仍在。把 Figma 面试临时从 completed 改为 canceled，确认只在档案显示“已取消”、日历不再出现；随后恢复 completed 并确认“已记录复盘”，原数据无变化。319×734 视口下保存按钮边界为 680–722px，直接点击成功。浏览器无错误；Career/Prompt lint、全量类型检查、4 项数据库契约测试与五环境 production build 全部通过。
- 结果：通过。界面不再把复制当保存、不把配置当连接、不把缺失输入的 AI 能力当完整功能，也不把已取消面试制造成待办压力。
- 下一轮候选：LinkedIn / BOSS直聘导入；面经 AI 使用当前未保存速记并回填可编辑草稿。

---

## 迭代 9 · 与招聘平台的联动以“可核对”为核心

### 体验证据

- 任务：把一段真实格式的 BOSS直聘分享文本粘贴到智能导入，解析现有 Arc · Design Engineer，并在保存前核对所有字段。
- 观察：DeepSeek 正确识别公司、职位、地点、关键词，并触发“可能已经收藏过”的重复提醒；全程没有读取平台登录态或自动保存。但工作方式直接显示英文 `remote`，结构化薪资对象被降成“待确认”，预览字段无法编辑；按钮却叫“核对后保存”，实际上用户只能整条接受或放弃。
- 影响：重复保护是真实价值，但不可编辑的 AI 预览会把小错误带进长期数据。招聘平台字段本来就不统一，尤其是 14 薪、月薪区间、远程/混合与分享文本里的 URL，必须在保存前落成用户看得懂、改得动的本地字段。

### 迭代产品稿

- 目标：LinkedIn / BOSS直聘联动不追求越权抓取，而是把平台的“链接或分享文本”可靠变成一份可编辑的本地收藏草稿。
- 方案：解析完成后建立独立导入草稿，规范化工作方式、来源与结构化薪资；从分享文本提取原链接；把 summary、职责、必需条件和加分项整理进职位描述，同时保留原分享文本便于追溯。公司、职位、地点、工作方式、薪资、关键词、链接与描述都允许保存前编辑。
- 重复策略：公司 + 职位规范化后命中已有记录时，明确显示已有职位；主按钮改成“保存一份副本”，不阻止用户记录同公司同名但不同团队的机会，也不默认合并。
- 隐私边界：优先推荐平台自带的“分享/复制链接”；只在点击解析后发送当前输入；不读取 Cookie、账号、站内消息或投递历史，不自动投递，不宣称官方 API 集成。单独 URL 的公共页面读取失败时仍可依靠粘贴文本。
- 验收标准：BOSS 40K–55K·14薪不再丢失，remote 显示并保存为“远程”；LinkedIn hybrid 显示为“混合办公”；所有核心字段可编辑；重复提醒准确；不点击最终保存时职位数仍为 6；重新打开导入不保留上次测试文本；分段按钮暴露当前状态；浏览器无错误。
- 本轮不做：不模拟登录、不安装招聘网站内容脚本、不批量抓取账号内 Saved Jobs；CSV 仍作为用户主动导出的批量入口。

### 开发与验证

- 改动：新增导入草稿规范化层，把 DeepSeek 的 `remote/hybrid/onsite` 映射为中文工作方式，兼容薪资 raw 或 min/max/currency/period/months 结构，并从分享文本提取原职位链接。解析后的公司、职位、地点、工作方式、薪资、关键词、链接与职位描述全部可编辑；描述按 summary/职责/必需条件/加分项整理，同时附上原始分享文本。公司 + 职位重复提醒会随编辑即时更新；按钮明确区分“保存到收藏”和“保存一份副本”。导入方式分段补充 pressed 状态，文案明确这不是官方 API 或登录态抓取。
- 工程修正：`tsconfig` 排除 `dist/.vinext/.wrangler` 生成目录，避免 production build 产生的 worker 源映射被下一次类型检查误当作应用源码。
- 测试：真实 BOSS直聘分享文本解析 Arc · Design Engineer，草稿得到地点“上海 / 远程”、工作方式“远程”、薪资“40K-55K·14薪”、5 个关键词与原链接，重复提示命中现有 Arc；公司临时改成“Arc 回归测试”后重复提示即时消失、主按钮变为“保存到收藏”，恢复 Arc 后提示和“保存一份副本”同时恢复。真实 LinkedIn 分享文本解析 Linear · Product Designer，`Hybrid` 规范化为“混合办公”，来源为 LinkedIn，原链接保留，并命中已有 Linear。两次都没有点击最终保存；关闭后职位仍为 6，重新打开输入为空且无旧预览。刷新后的浏览器无新错误；Career lint、全量类型检查、4 项数据库契约测试与五环境 production build 全部通过。
- 结果：通过。与 LinkedIn / BOSS直聘的联动从只读 AI 结果变成可核对、可修正、可追溯的本地收藏草稿，没有扩大平台权限或制造“官方接入”错觉。
- 下一轮候选：面经 AI 使用当前未保存速记并回填可编辑草稿；职迹完整附件备份。

---

## 迭代 10 · 面经 AI 必须接住刚写下的内容

### 体验证据

- 任务：打开一条面经，在原始速记、总结或复盘中写入尚未保存的新内容，直接点击“AI 整理速记”，再尝试把结果带回当前面经。
- 观察：当前表单的 summary/raw_notes/reflection 是 uncontrolled input；AI 按钮发送的是打开抽屉时的持久化 interview 快照，只额外带了受控 questions。刚写的速记必须先保存、再重新打开才可能被模型看到。生成结果进入通用预览后也只有复制，没有回填目的地。
- 影响：用户最需要 AI 的时刻正是刚结束面试、快速倾倒记忆的时候；要求先保存再调用会打断记录，还可能让模型整理旧内容。复制 JSON 再手工拆回多个字段，比自己整理更麻烦。

### 迭代产品稿

- 目标：用户可以先自然写速记，再让 AI 基于“此刻表单里的全部内容”整理；AI 结果只填入本次尚未保存的草稿，不绕过最终确认。
- 方案：总结、原始速记、状态与复盘全部改为受控草稿；调用时发送当前 summary/raw_notes/questions/reflection。通用 AI 预览在本场景增加“填入面经草稿”，把 summary、问题、我的回答、追问/改进建议与复盘段落映射回对应字段；原始速记永远保留。
- 可逆性：填入前保存当前表单快照，显示“已填入 / 撤销整理”；撤销恢复 AI 前的未保存内容。关闭抽屉或点击取消不会写数据库；只有“保存面经”才落库。
- 验收标准：在 raw notes 加入唯一临时标记后直接调用，发送 payload 含该标记；AI 预览出现“填入面经草稿”；填入后字段可编辑且原始速记不变；撤销精确恢复；保存、刷新后结果持久化。测试完成后恢复 Figma 原始数据；浏览器无错误。
- 本轮不做：不自动猜测面试官反馈，不把 `better_answer` 当作用户真实回答，不自动将状态改为已完成。

### 开发与验证

- 改动：面试状态、总结、原始速记、问题/回答/备注与复盘全部改为受控草稿，AI 请求在点击瞬间发送当前表单快照。面经场景的 AI 预览新增明确的“填入面经草稿”，只回填总结、问题和带来源标签的复盘建议；原始速记与状态不受 AI 结果影响。已有回答永远优先保留；模型生成的 `my_answer` 只有能在原始速记中逐字找到时才可进入回答，否则只作为“AI 整理的回答候选（待确认）”进入备注，`better_answer` 始终标为“AI 改进建议”。填入前深拷贝可变字段并提供一次撤销；用户随后手改这些字段会让旧撤销失效，避免吞掉新输入。新增严格白名单解析、长度/数量上限、异常不部分回填、应用失败不关闭预览，以及 AbortController + 请求 ID 防止并发结果串线。
- 测试：新增 4 项纯函数契约测试，覆盖保留用户回答、原文依据判断、畸形/空结果拒绝、集合与文本边界；连同本地数据库契约共 8 项全部通过。真实浏览器在 Figma 面经的 status、summary、raw notes、问题与 reflection 写入 4 处未保存标记并临时选择 canceled，DeepSeek 返回同时识别标记、跨时区速记和 canceled 冲突，证明请求使用当前表单而非 SQLite 旧值。点击填入后总结/问题/复盘更新，原始速记与状态保持不变；撤销精确恢复 4 处标记。关闭不保存、重开后标记为 0 且数据库仍为 completed 原值。第二次完成“AI 填入→用户手改总结→保存→关闭重开”，确认 AI 复盘、手改总结与速记均持久化；随后逐字段恢复 Figma 原始数据并重开核对无测试标记。319×734 真实截图确认上下文说明、44px 表单控件与固定保存栏没有遮挡。Career lint、全量类型检查、8 项测试与五环境 production build 全部通过。
- 结果：通过。AI 现在接住用户刚写下的内容，但仍停留在可核对、可撤销、需手动保存的草稿边界内；模型措辞不会自动冒充用户经历。
- 下一轮候选：职迹完整附件备份；联系人详情与编辑；最终冻结前无障碍回归。

---

## 迭代 11 · 备份必须带走真正的材料原件

### 体验证据

- 任务：在设置中导出职迹备份，假设浏览器资料被清除，再判断能否只用这个文件恢复简历、作品集与案例附件。
- 观察：当前按钮明确只导出 SQLite；材料表里的名称、版本与 `file_key` 会被带走，但 OPFS 中的实际字节不会进入备份。恢复后界面仍可能显示“本地附件”，点击时却找不到原件。
- 影响：诚实提示避免了虚假安全感，但仍把最关键的恢复工作留给用户手工完成。对私人本地应用而言，浏览器资料清除正是最需要备份的时刻；只有索引而没有原件的材料记录不算完整恢复。

### 迭代产品稿

- 目标：一个 Career 备份文件同时包含 SQLite 与所有被材料记录引用的 OPFS 原件；导出前逐个校验，恢复前完整验证，任何损坏都不能覆盖现有数据。
- 格式：使用版本化的 `.career-backup` 二进制容器：固定魔数、受限长度的 JSON manifest、SQLite 字节和顺序附件字节。manifest 记录产品/版本、导出时间、SQLite application ID / user version，以及数据库和每个附件的 byte size、SHA-256 与原始元数据；manifest 自身也有基于固定投影的 SHA-256，不包含 API key、模型日志或隐藏状态。
- 导出：只收集数据库中真实引用的 Career 附件；缺失、大小不符或 hash 不符时整体失败并指出问题，不生成“成功”文件。文件是明文备份，界面明确建议安全保管。
- 恢复：先限制格式/数量/大小，再完整验证 SQLite 头、身份、manifest 与每段 SHA-256；验证全部通过后把附件以新 UUID 流式写入暂存，把数据库写入不可变 candidate generation，并在 candidate 的单一事务中校验材料行与 manifest 后重写引用。candidate 关闭重开并通过完整性、外键、必需表列、trigger/view 白名单及 canonical identity 后，才写入带序号和 checksum 的 A/B 活动指针。切换前 live generation 从不被覆盖；成功后暂时保留上一代数据库与原附件作安全回退。旧版 `.sqlite3` 也走 candidate 路径，并清空没有原件可恢复的附件索引。
- 跨标签页：所有 Career 读写、材料文件操作、导出与恢复共同参与同一 Web Lock；代际切换通过 BroadcastChannel 通知其它标签页重载，避免旧连接继续写入上一代数据库。浏览器不支持 Web Locks 时拒绝执行完整备份，不静默降级成不一致快照。
- 验收标准：无附件也能导出合法完整备份；带多个 Unicode 文件名附件时 round-trip 后内容 hash、MIME、名称和材料关联一致；篡改 manifest、数据库或任一附件都会在覆盖前失败；在 candidate 写入、校验与指针切换前后的失败结果只能是全旧或全新；备份不含 DeepSeek key；移动端可看懂“完整备份 / 旧版 SQLite”的区别。
- 本轮不做：不把明文附件发往服务器；不以尚未经过密码学与大文件测试的自制加密冒充“加密备份”；跨三个应用的总套件备份留到各产品格式稳定后。

### 开发与验证

- 改动：新增带固定魔数、严格 manifest、manifest 自身摘要、SQLite 身份与逐段 SHA-256 的 `.career-backup` 容器；导出只打包数据库真实引用且元数据完全一致的材料原件。Career 数据库与文件操作统一进入 Web Lock，恢复先把附件写入新 UUID，再把数据库写入不可变 generation；附件引用校验与映射在 candidate 的单一事务中完成。Worker 新增 READY/token/hash、关闭重开复检、历史激活标记和带 checksum 的 A/B 指针；失败时 live generation 不被原位覆盖，激活后通过 BroadcastChannel 让其它标签页重载。旧 SQLite 同样进入 candidate，并主动清除无法恢复的附件索引。设置页改为可键盘聚焦的原生文件入口，文案只承诺“交给浏览器下载”，材料页也补上真实删除与文件清理闭环。
- 测试：格式层 48 项覆盖 Unicode、多附件、同长度合法 JSON 篡改、SQLite 身份伪造、段 hash、严格字段及 2 GiB/1000 文件边界；恢复计划 9 项覆盖精确 key 集合、文件名/MIME/大小、零附件、重复/交叉 key 与事务回滚；代际导入 5 项覆盖坏指针回退、首次 legacy baseline、stage 失败、激活与 Vocab 隔离。连同 AI 与本地存储契约共 70 项全部通过，Career/全量 ESLint、TypeScript 与五环境 production build 通过。319×734 真实浏览器完成无附件导出，并通过原生 file chooser 上传一个末字节被篡改、包含 Unicode 材料的备份；界面明确报告附件 SHA-256 不匹配，随后“活跃机会”仍为原来的 6，证明损坏文件在写入前被拒绝。由于当前连接浏览器无法创建安全隔离 origin，未用有效测试备份替换主 origin 的真实资料；有效多附件 round-trip 由格式、事务与代际测试覆盖，并保留为发布前独立浏览器 profile 的人工演练项。
- 结果：通过。完整备份现在带走真正的材料原件，恢复从“尽力回灌”升级为可验证、可回退的代际切换；对用户可见的下载、明文与旧版限制都保持诚实。
- 下一轮候选：联系人详情、显式职位关联与真实沟通记录。

---

## 迭代 12 · 联系人不是销售漏斗，只记录真实关系

### 体验证据

- 任务：新增一位联系人，回看与他的职位关系、最近一次真实交流和下一步，再尝试修改或归档。
- 观察：当前卡片只有姓名、公司、手填时间和“创建待办”。新增表单允许填写“最近联系”，却没有任何沟通内容作为证据；“创建待办”还会按公司名暗中关联第一个职位。同公司有多个岗位时可能关联错误，联系方式保存后也没有详情入口。
- 影响：界面看似完整，实际把无证据时间展示成真实联系，把系统猜测展示成用户选择；红色过期跟进又把私人关系做成催促型 CRM。它既不可靠，也不照顾求职过程中的心理感受。

### 迭代产品稿

- 目标：联系人页只回答“这个人是谁、与哪些职位明确相关、真实发生过什么沟通、我是否主动安排了下一步”。不增加关系评分、回复率、冷却天数、联系频率 KPI 或失联提醒。
- 列表：导航与页面统一称“联系人”。卡片展示最多两个明确职位关联、最近一条真实联系和最早一条联系人待办；没有记录时写“等一次真实沟通”，没有待办时写“没有安排下一步”。原计划已过时只说“可重新安排”，不使用红色欠债语义。联系人与已归档分段不显示数量。
- 详情：具名卡片打开详情抽屉，首要动作是“记录联系”，其次是编辑。抽屉依次呈现下一步、明确职位关联、真实联系时间线与联系方式；邮箱和电话只打开系统应用，绝不自动写一条联系记录。空时间线明确“不需要为了填满而补写”。
- 记录联系：发生时间、渠道、方向、摘要和关联职位均由用户核对；摘要必填。可选“顺手安排下一步”，联系记录、显式职位关联与联系人待办在一个事务内保存，任一步失败都不留下半条数据。
- 编辑与归档：新增/编辑只管理基本资料和显式多选职位，不再填写“最近联系 / 下次跟进”。归档保留历史、关联与待办，支持即时撤销和从已归档恢复；本轮不提供永久删除。
- 兼容：数据库升级到 v2，但不把旧 `last_contact_at` 冒充联系记录，也不把旧 `next_follow_up` 自动生成任务。旧时间只作为待确认历史提示，由用户决定是否补录或转成待办；职位里的旧 `contact_name` 只标作旧版备注，绝不按姓名或公司自动匹配。
- Apple UI 收尾：修复透明文件入口焦点环、移动侧栏的焦点进入/圈定/归还、背景 inert、滚动锁和 Escape；“更多”真正打开完整导航。统一功能灰字对比度与 44px 热区，移除设置锚点永远高亮第一项的假状态；有 0 个逾期待办时也不再用责备式表述。
- 验收标准：新建联系人不会产生“今天联系过”；同公司两个职位只关联用户明确选择的一项；联系记录与可选待办原子写入；点击邮箱/电话不改变数据库；归档恢复不丢任何历史；v1→v2 不新增伪记录；319×734 下详情、表单、职位多选与固定操作区无溢出，键盘可完成全部闭环。
- 本轮不做：不自动发消息，不读取 LinkedIn/BOSS 登录态或站内消息，不根据公司猜职位，不永久删除联系人，不用 AI 猜测关系质量。

### 开发与验证

- 改动：Career canonical schema 升级到 v2，新增显式联系人—职位关系、真实沟通记录和带 `contact_id` 的待办；创建、编辑、互动、可选下一步、归档与恢复统一进入 Career Web Lock 和事务。v1/v2 备份都可在 candidate generation 内迁移并严格复检；旧最近联系、旧提醒和职位自由文本不会生成伪互动、伪待办或伪关系。联系人导航、列表、详情、记录联系、安排下一步、编辑和归档恢复完成独立 Apple 风格界面；职位详情增加真实关系回链，未确认文本明确标成“旧版联系人备注”。首页删除重复的第二个 AI 准备入口，避免功能越多越打扰。
- 测试：新增 18 项联系人事务、迁移和锁契约，连同备份、代际恢复、AI 与本地存储共 79 项全部通过；Career scoped ESLint、全量 TypeScript 与五环境 production build 通过。319×734 真实浏览器只读验收 3 位既有联系人：v1→v2 后没有凭空出现互动、关系或任务；列表、已归档空状态、详情、真实联系表单、可选下一步和职位详情均无横向溢出。嵌套表单打开后上一层抽屉被 inert，关闭后焦点回到“记录联系”；再关闭详情后焦点回到原联系人卡，页面滚动锁被清理。主 origin 未写入测试联系人；写入/回滚由独立 SQLite 测试覆盖，避免污染用户资料。
- 结果：通过。联系人从手填时间卡片变成有证据、可回看、可撤销的私人关系记录；界面不统计关系价值、不催促跟进，也不把系统猜测包装成用户事实。
- 下一轮候选：冻结职迹，切换到拾词的真实阅读与播客闭环验收。

---

## 迭代 13 · 拾词只记录真实发生的学习

### 体验证据

- 任务：以第一次打开拾词的用户身份浏览首页、文章、播客、点词解释、复习与设置，再检查这些界面承诺的数据是否真的可恢复、可撤销且符合隐私开关。
- 观察：新数据库会自动写入样例文章、播客、单词、今日复习次数与学习时长，首页却把它们呈现为个人历史；“完整备份”只带 SQLite，不带 OPFS 音频；本地锁开启后远程播放器仍会请求媒体；有字幕时勾选转写仍会上传；点 A 后立刻点 B，A 的迟到解释可能覆盖并保存到 B。每日新词上限没有进入队列，已掌握的词仍会复习，撤销评分不撤销统计；文章进度、书签、播客续播与收听时间也没有对应界面所暗示的真实性。
- 影响：这些问题不是功能少，而是用户无法区分自己的行为与演示数据，也无法相信隐私开关和备份。再增加 streak、目标环或更多统计只会放大压力和不确定性。

### 迭代产品稿

- 目标：把拾词收敛成一间私人英文阅读与听读室：点击语境中的词即可理解，愿意时再保存和复习；没有虚构历史、没有欠账、没有失真的隐私或恢复承诺。
- 方案：全新 profile 保持个人数据为零；文章与播客记录真实位置、可见阅读时间和实际播放时间；一次点词自动解释，并用请求身份与取消机制阻止串词。英文解释优先，中文按需获取；异常模型结构不入库。复习采用同一纯函数生成预告和实际到期时间，旧卡优先、每日上限只约束新卡，已掌握/忽略会暂停，撤销同时恢复卡片与统计。
- 数据边界：给当前 runtime 表建立正式 application id / user version 与版本化迁移；所有 Vocab 读写、文件、导出与恢复进入同一 Web Lock。`.vocab-backup` 必须同时包含 SQLite、所有被引用的本地音频、manifest 与逐段 hash，并通过不可变 generation + A/B 指针在完全验证后激活；旧 SQLite 也先 stage，不能原位覆盖。
- 隐私边界：本地锁开启时，URL、RSS、AI、转写与远程音频都不得发起请求；外部转写只有服务真实配置、用户明确选择且确实没有字幕时才可上传。设置页区分“检测到配置”“真实调用成功”和“未配置”，不以环境变量存在冒充连接可用。
- 心理体验：删除连续天数、隐藏的 18 分钟目标环、红色欠卡、正确率和完成率；改成最近真实阅读/收听、回到语境的次数与内容分布。零数据仍是一种完整状态，不评价自律。
- Apple UI：统一字阶、对比度、44px 热区与 safe-area；导入、点词、词详情、搜索和移动侧栏都具备名称、初始焦点、Tab 圈定、Escape、背景 inert、滚动锁与焦点归还；319×734 不横向溢出。
- 验收标准：全新 profile 统计全部为零；文章/播客的真实位置与时间跨刷新恢复；本地锁下远程媒体 0 请求；有字幕时转写 0 请求；A/B AI 乱序不串词；新卡上限、当地午夜、已掌握/忽略、撤销与特殊字符 cloze 通过固定时钟测试；含音频备份可完整 round-trip，任一损坏不切换 active generation；移动与键盘闭环通过。
- 本轮不做：不引入排行榜、连续打卡、词汇量评分或 AI 生成的学习表现评价；不把未配置的 DeepSeek chat 冒充语音转写；不在拾词稳定前开始健身平台开发。

### 开发与验证

- 改动：Vocab runtime 建立正式 SHCI application id、v2 user version 与严格 migration ledger；全新数据库不再写演示内容，旧样例只按固定 ID 一次性清理，并在外键安全的前提下保留真实 occurrence 与 review history。所有读写进入同一 Web Lock 并通过 BroadcastChannel 同步。SRS v2 用同一纯函数生成界面预告与实际计划：每日上限只限制新卡，已掌握/忽略及缺少有效英文解释的卡会暂停，补齐解释可恢复，撤销同时恢复卡片与统计；当地日期、特殊字符 cloze 与重复书签均有明确契约。文章记录真实阅读位置与可见时间，播客记录实际播放位置与收听秒数；无时间轴的纯文本字幕不再伪造同步时间。一次点词自动请求英文解释，严格解析模型结果，并用 AbortController、请求序号与 selection identity 防止 A/B 串词；中文解释仍按需获取。导入流程只有在确实没有字幕、服务已配置且用户主动勾选时才可能上传音频；本地锁同时阻止 URL、RSS、AI、转写与远程媒体。新增 `.vocab-backup`：SQLite 与所有真实引用的本地音频一并打包，manifest 自校验、逐段 hash、v0/v1/v2 候选迁移和 A/B generation 激活；旧 SQLite 也先 stage，并只清空无法随文件恢复的本地音频引用。设置页展示浏览器真实持久化状态，下载只承诺“已交给浏览器”，明文与旧版限制明确可见。整体补齐 Apple 风格字阶、对比度、44px 热区、safe-area、reduced motion，以及搜索、导入、点词、词详情和移动导航的焦点圈定、Escape、inert 与焦点归还。
- 测试：整仓 ESLint、TypeScript 与 vinext 五环境 production build 全部通过；128 项自动测试覆盖空白新库、v0/v1→v2、精确旧 seed 清理、真实历史保留、SRS 各评分/新词上限/当地午夜/撤销/暂停恢复/特殊字符、跨标签锁、AI 严格解析，以及完整备份的 0/1/多音频、Unicode、损坏、错库、未来版本、缺失对象、映射回滚、激活失败/响应丢失/状态不明和 Career 隔离。319×734 真实浏览器确认页面宽度始终为 319，无横向溢出；旧样例文章、播客和词条已从主 origin 消失，空资料库与空词库有完整状态。真实 DeepSeek 点词 `cyclist` 返回英文语境解释；紧接着 A→B 并关闭请求时，A 的迟到结果没有覆盖 B。搜索、移动导航与点词弹层均验证初始焦点、背景 inert、Escape 和精确焦点归还。设置页在浏览器拒绝持久化时明确显示“仍可能被清理”；无音频完整备份真实导出并报告 0 个本地音频；本地锁下转写 checkbox 被禁用且说明“不会上传音频”；健康检查只报告服务端配置且明确没有发送文章或词语。
- 结果：通过并冻结。拾词不再把演示数据、模型迟到结果、未发生的学习时间或不完整备份包装成用户事实；阅读、收听、点词、复习、隐私与恢复形成了可核对、可撤销的完整闭环。
- 下一轮候选：进入器械约束驱动的健身规划与日历产品稿；继续坚持“不评分自律、不制造欠账、计划服从真实环境”。

---

## 迭代 14 · 先问健身房有什么，再谈训练计划

### 体验证据

- 任务：拿一份常见的“每周训练计划”走进不同健身房，逐项核对动作所需器材、哑铃是否成对、杠铃片能否组合出建议重量，以及高峰时器材不可用后的替代路径。
- 观察：只记录“有哑铃 / 有杠铃”仍然不够；数量、离散重量、杆重、每侧片数、器材状态与所属场地都会改变动作是否真正可做。标准计划还会把不同场地的器材混在一起，并在能力未知时给出看似精确的起始重量。
- 影响：计划越详细，不代表越有用。无法在现场执行的精确数字会增加录入与临场决策负担，还会让用户误以为是自己没有完成计划。

### 迭代产品稿

- 目标：规划的最小单位不是“动作名称”，而是“当前场地 + 可用器材实例 + 真实重量能力 + 明确的自重路径”。任何未知值保持未知。
- 方案：先建立场地，再录入器材类型、数量、状态、负荷语义与离散档位；弹力带等级、无单位面板档位等相对阻力只进入备注，不伪装成公斤。动作库使用固定本地 ID 与多资源需求，普通双手哑铃动作必须有一对同档哑铃，杠铃总重必须能由对称片数组成。
- 规划：本地规则先生成可执行草稿，并同时校验器材归属、全部资源、时间预算、身体避用边界和替代动作。没有可比较历史时只给 RIR 校准规则，不猜起始公斤数。AI 只返回草稿，动作与器材都必须引用本地允许池并再次通过同一校验器。
- 隐私：发送 AI 的内容只保留规范化器材类型/状态/数量/重量、目标频次和能力数字；场地名称、自由备注、身体部位与边界文字不外发。用户填写的 avoid 通过本地过滤动作池生效，modify/monitor 只在本地显示原文提醒。
- 情绪边界：不提供训练完成率、连续天数、准备度分数或“落后”状态；计划是可修改的版本，不是必须偿还的任务。
- 验收标准：两个场地的器材绝不串用；单只普通哑铃不能生成双手动作；不存在的重量不能进入计划；器材维护和身体 avoid 会阻止未来草稿与现场选择；AI 返回未知动作/器材或猜测重量时整份拒绝；无 AI 时全部核心规划仍可用。
- 本轮不做：不把器材照片直接交给模型并自动入库；不根据身体文字给医学诊断；不以通用动作库替代现场事实。

### 开发与验证

- 改动：新增独立 `shilian.sqlite3`、SHLN application id、17 张业务表、严格 schema/ledger/FK 校验与零个人 seed；场地、器材、离散重量、偏好、身体边界、版本化计划、日历、训练、组记录、能力和文件引用均有独立事务 API。动作 catalog、器材可执行性 helper、本地 planner 与 AI parser 共用同一真值边界。首页加入第三个 Apple 风格入口，适练拥有独立 UI、CSS、AI 路由、OPFS 文件空间和备份产品身份。
- 测试：真实 SQLite 覆盖空库身份、跨场地隔离、成对哑铃、杠铃片组合、维护状态、身体边界、计划版本和排期；planner 覆盖多资源、时间预算、离散重量、未知能力与稳定输出；AI 覆盖允许动作池、未知引用、身体不适保守结束和最小输入。TypeScript、ESLint、production build 与全仓测试通过。
- 结果：通过。计划不再从抽象“最佳动作”出发，而是从用户此刻真正拥有的训练条件出发。
- 下一轮候选：把日历、现场替代、组记录、历史详情与完整备份闭成一条不会丢事实的链路。

---

## 迭代 15 · 实际训练不是计划的完成勾选

### 体验证据

- 任务：在计划动作尚未做完、器材被占、时间不足、心肺只记录时长、出现不适和中途结束等情境下完成一场训练，再从日历与历史回看实际发生了什么。
- 观察：如果结束训练时把所有计划动作都串成“实际动作”，跳过会被伪装成完成；替代动作若只靠临时状态标记，结束后会丢失“原动作 → 实际动作”的关系；心肺沿用力量表单会无法保存；不适原文如果只在现场出现，之后无法核对。保存后的页面刷新失败也不能被描述成“写入失败”。
- 影响：训练历史会反过来影响能力与后续计划。任何伪完成、丢替代、假重量或重复提交都会把下一次规划建立在错误事实之上。

### 迭代产品稿

- 目标：计划、现场选择与实际记录始终是三层数据。历史只展示有真实组/时长的动作，或明确保存的替代关系；未做计划项只作中性说明，不形成欠账。
- 现场：力量记录重量/次数/RIR，心肺记录时长/RPE；每次提交有稳定幂等标识，撤销只删除对应一条。替代保留原动作、实际动作与用户原因；器材和身体边界在开始、添加、替代与保存计划时都重新校验。
- 不适：原文与对应动作/组一起保存，并明确“这是用户当时的记录，不是诊断”；不适组不进入能力记录，也不会自动创建身体边界或修改计划。
- 历史：详情按真实日期、场地、时长、实际动作和组事实呈现；替代但未记录组数时写明“仅保存替代”，不补成完成。复盘只有明确点击保存才写入，失败保留草稿；写入成功但刷新失败要明确“已保存，请刷新”，不能诱导重复提交。
- 日历：改期只移动计划；不进行不会堆到明天；完成、不进行、取消与进行中分别展示。当前身体 avoid 与旧计划冲突时保留旧版作参考，但禁止再次排期并引导生成适用版本。
- 数据安全：完整备份同时包含 SQLite 与所有 ready 器材照片，逐段校验后写入 candidate generation，只有完整 schema、外键、文件元数据与 hash 全部通过才激活；旧 SQLite 恢复会清除无法随文件恢复的引用。
- 验收标准：中途结束不制造实际动作；替代关系结束后仍可回看；时长型心肺可保存且不产生力量能力；带不适的组保存原文且能力为零；保存中无法误关弹窗；损坏备份不切换 active generation；319px 下主页、首启、五个主页面和设置无横向溢出。
- 本轮不做：不自动根据不适生成诊断或训练处方；不把“未进行”当失败；不承诺没有生成可下载副本的跨代附件回滚。

### 开发与验证

- 改动：完成现场训练状态机、动作添加/替代/跳过、力量与计时组、幂等记录、撤销、提前结束、能力过滤和真实 toast；完成滚动周排期、DST 安全日期、计划冲突提示、归档场地恢复与身体边界暂停/恢复。器材照片采用持久两阶段写入与启动 reconciliation；完整备份使用自校验容器、Web Lock、不可变 candidate 与 A/B 指针。训练历史新增可访问详情、替代事实、不适原文与显式复盘编辑；四个持久表单在真正写入期间阻止关闭，非法本地输入仍可退出。Career 补回主页入口，离线 shell 补入 Fitness。
- 测试：全仓 242 项自动测试通过，包含 Fitness schema/store/planner/AI/files/backup/form/time 与跨产品隔离；ESLint、TypeScript 和 vinext 五环境 production build 通过。真实 319px 浏览器逐页验证主页、适练首启、今日、计划、日历、场地、记录、设置与职迹主页链接，`scrollWidth === clientWidth`；首启 Escape 回到动作搜索，建立场地弹窗失去原 opener 时焦点回到当前导航。
- 结果：通过。适练现在保存的是训练事实，不是计划表上的完成想象；恢复、历史与下一次规划都以同一组可核对数据为基础。
- 下一轮候选：用独立浏览器 profile 做带多张照片的完整 round-trip 与多标签故障注入；继续缩短现场单手操作路径。

---

## 迭代 16 · 一次事实，处处一致

### 体验证据

- 任务：把同一计划日改期、标记不进行或取消后再次安排一周；从不同场地打开当天计划；误触开始训练后尝试退出；连续记录两组，其中只有第一组带不适备注；再在另一个标签页恢复备份，并回到套件首页考虑继续增加新工具。
- 观察：仅靠可变的 `starts_at` 和滚动时间窗判断重复，会让已经完成、不进行或取消的计划日再次生成；计划场地与当前场地不一致时直接开始，会悄悄改写用户当下的训练条件。空训练只有“完成”出口，一次不适备注可能被带到下一组；切换器材模板还可能覆盖用户已经改过的名称或数量。恢复后的 durable generation 已经改变，另一个标签页的 Worker 却可能继续握住旧句柄。首页仍把当前产品数量写进文案和布局，入口、快捷方式与离线 shell 也缺少一个共同来源。
- 影响：这些问题单独看只是一个重复日程、一次多余点击或一条过期缓存，合在一起却会让计划事实、现场事实和恢复后的事实在不同页面出现多个版本；硬编码“有几个应用”也会让每次扩展都变成一次容易漏项的全站修改。

### 迭代产品稿

- 目标：同一件事只拥有一个稳定身份，用户当下的选择必须先于系统推断；备份恢复后所有标签页读取同一代数据。套件首页以“空间”为可扩展单位，不再把当前数量写成产品承诺。
- 日历身份：每个计划日 occurrence 在首次排期时写入不可变的本地日期 `occurrence_key`；改期只改变预约时间，不改变它来自哪一次计划。完成、不进行和取消都是这个 occurrence 的最终事实，再次生成一周不能把它复活。SHLN 升级到 v2，精确 v1 数据在单个事务内迁移；若旧数据存在身份冲突，整次迁移回滚，版本、迁移账本与原记录都保持不变。v1/v2 备份都先作为候选验证，激活后统一成为 canonical v2。
- 现场意图：计划场地与当前场地不一致时，不替用户猜测，而是提供“切换到计划场地并开始”和“按当前场地开始临时训练”两个明确出口；后者保留原日历安排。没有任何组或时长的误开训练可以取消：来自日历的事件回到待进行，手动误开不留下空历史；只有用户明确选择“保存空时段”时才保留。已经存在组、时长或其他训练事实时拒绝走取消捷径。
- 一次性输入：不适备注只属于当前准备提交的这一组，持久化成功后清空，失败时保留以便重试；它仍不是诊断。器材模板只为尚未编辑的名称和数量提供建议，一旦用户改过对应字段，切换模板不再覆盖；普通哑铃的初始数量按一对建议，但最终事实始终由用户确认。
- 恢复一致性：SQLite Worker 在每次数据库请求前重新读取 A/B durable generation 指针；指针变化时先打开并验证新一代，再关闭旧句柄。跨标签通知可以帮助界面刷新，但不再承担数据正确性的唯一责任。
- 空间架构：首页的卡片和快捷入口共同读取 typed `suiteSpaces` registry；名称、路径、文案、预览和可访问标签由同一份配置生成，离线 shell 通过契约测试与 registry 保持一致。标题改为“你的空间，各自专注”，入口采用 Apple 风格的安静层级、44px 快捷热区与 `auto-fit` 自适应网格；删除全局 CSS 中已被各产品独立样式替代的旧 Career/Vocab 原型，只保留 reset 与套件入口样式。
- 情绪成本审查：误触不应被解释成完成，取消也不应变成欠账；“保存空时段”必须是显式选择。首页不强调已经有几个工具，而是让每个空间独立、平等地出现，新增空间不会让旧空间显得被挤压或落后。
- 验收标准：同一计划日无论改期或进入最终状态都不会重复生成；v1→v2 冲突不产生半迁移；空训练的取消与保存是两个不同动作；跨场地开始前必须选择上下文；不适备注不串组，器材身份不被模板覆盖；恢复后旧标签下一次请求切换到新 generation；新增空间只需扩展 registry 与对应离线入口，首页不再修改数字文案或复制渲染逻辑。
- 本轮不做：不自动合并存在 occurrence 冲突的旧日历，不替用户选择训练场地，不把有真实记录的训练伪装成误开删除；不把三个现有空间强行统一成共享 UI，也不为未来空间预先制造空卡片。

### 开发与验证

- 改动：SHLN canonical schema 升级到 v2，为计划日加入不可变 `occurrence_key`、唯一约束和原子 v1→v2 迁移；运行时与完整备份恢复都兼容精确 v1/v2 输入并统一复检 canonical v2。新增空训练事务取消能力及独立“保存空时段”入口；今日与日历共用场地感知的开始流程，不同场地时由用户二选一。组记录采用一次性不适草稿；器材名称/数量改为 dirty-aware 模板建议。数据库 Worker 改为请求级核对 durable generation。套件首页改为 typed 空间 registry 驱动卡片与快捷入口，并清除不再使用的全局产品原型 CSS。
- 测试：`fitness-storage` 与 Fitness backup plan/format/service 契约覆盖 v1→v2 成功、身份冲突全回滚、改期保持 occurrence、最终状态不重生、空训练取消回到正确日历状态，以及已有事实时拒绝取消；`fitness-ui-contract` 覆盖跨场地二选一、并发开始去重、一次性不适与空时段双出口；`fitness-forms` 覆盖器材名称/数量的 untouched/dirty 组合；local DB generation import 契约覆盖另一标签恢复后下一次请求切换指针；suite navigation 契约覆盖 registry 双入口、离线 shell 一致性和全局 CSS 边界。相关提交均通过各自的 diff 检查后进入 Git 检查点。
- 结果：通过。日历 occurrence、现场开始、组备注、器材身份与恢复 generation 都有了单一真值；首页也从写死当前数量的“三个应用”变成可以继续生长的私人空间入口，同时保留三款产品各自独立的 UI。
- 下一轮候选：用独立多标签浏览器 profile 做恢复中断与旧标签长驻故障注入；继续以真实单手训练路径审计现场按钮顺序、失败恢复和运动中可读性。

---

## 迭代 17 · 安静的一天不需要被填满

### 体验证据

- 任务：以一位暂时没有新面试、没有 Offer，或刚结束一场面试还不想立刻复盘的用户身份打开职迹；再用早期版本留下的本地数据库与一份未保存面经，检查第一眼看到的事实和关闭路径。
- 观察：全新数据库会自动写入 6 个公司、一个 Figma Offer、已完成面试、联系人、材料和待办，且没有说明它们是演示；没有真实已安排面试时，Today 仍会取任意职位拼成“这次面试”并向 AI 发送 `interview: null`。面经一旦点遮罩、Escape 或关闭就会丢失刚写的速记；浏览器草稿若基于旧版记录，直接恢复又可能覆盖另一个标签页刚保存的内容。
- 影响：假 Offer 会在用户正承受求职不确定性时冒充个人结果；空白被包装成“还应该做什么”，会让私人工作台制造压力。草稿丢失或静默覆盖则会让用户不敢相信工具真的替自己保存了经验。

### 迭代产品稿

- 目标：空白、等待和暂时不复盘都属于正常状态。职迹只能展示用户真实记录或明确安排的内容，不用演示人生、指标或 AI 猜测把页面填满。
- 空白真值：全新 Career SQLite 只创建流程阶段，职位、任务、面试、联系人、材料、活动与目标全部为零。旧版演示数据只有在逐字段、逐关系完全匹配且确认从未被改动时才可原子清理；任何偏离、附件或真实互动都完整保留，并在每个页面最前面说明“可能含示例，未自动删除”。
- 今日落点：只由真实 `scheduled` 面试、明确 `todo` 待办或需要用户决定的 Offer 驱动；没有时直接说“今天没有必须处理的事”。时钟按分钟与标签页重新可见时更新，不让跨午夜页面继续显示昨天。
- 面经草稿：所有关闭路径都先判断 dirty，并提供“继续编辑 / 保存本机草稿并关闭 / 放弃修改”三项明确选择。本机草稿不进入 SQLite 或导出备份；清除站点数据会删除。若正式记录的 `updated_at` 已变化，旧草稿不得自动填入，只有用户明确选择“载入本机草稿核对”后才应用。
- 写入反馈：待办、面试与面经使用同步 busy guard；数据库写入成功但列表刷新失败时明确说“已保存在本地”，不把它伪装成写入失败并诱导重复提交。阶段历史只写“阶段改为”，不使用“推进至未通过”。
- 心境边界：不把没有行动叫落后，不把面经默认写成自我纠错；问题备注与复盘使用“想保留的线索”“这次想记住什么”等中性自述。旧资料提示不指控用户，也没有冒险的一键删除。
- 验收标准：fresh runtime 个人表全部为零；严格旧演示匹配可清理、任一偏离完整保留；零事项 Today 不出现面试文案或 AI 入口；重复提交只有一次 SQLite 写入；dirty 面经任意关闭路径都不能无提示丢失；stale 本机草稿在点击载入前不得改变任何表单字段；319px 无横向溢出且可见按钮至少 44px。
- 本轮不做：不猜哪些保留下来的旧记录属于用户；不自动保存面经正文到远程服务；不在数据库生命周期迁移和备份兼容完成前开放新的归档/恢复交互。

### 开发与验证

- 改动：Career runtime 改为零个人 seed，参考 zhiji schema 同步清除个人示例；新增完整旧 seed 指纹、事务保护和 `review-needed` 状态。Today 改用纯选择器与实时 clock；去掉 `interview:null` 回退；修正 undo 成功文案和创建去重。面经加入本机草稿、dirty 三选、beforeunload 保护、stale 冲突选择与正式保存清理；旧资料保护提示提升到所有 Career 页面顶部。
- 测试：9 项真实 Career SQLite 场景覆盖空库、未改演示清理、v1→v2 contacts、单字段偏离、新增职位、附件引用、真实互动、非演示资料与事务/OPFS 边界；7 项生命周期事务计划覆盖 future-only pause、明确 keep、操作来源、用户后改不恢复与终态保护；9 项 UI 契约覆盖 quiet Today、实时 clock、undo、同步 busy、dirty 关闭、stale 草稿与全局 legacy 提示。全仓 305/305、ESLint 与 TypeScript 通过；真实 319px 浏览器确认 `scrollWidth === clientWidth === 319`、提示在首屏可见、按钮热区与草稿流程正常。
- 结果：通过并分为 `33e37c7`、`d512e17`、`da9286f`、`85d77b3` 四个可回退检查点。职迹现在允许一天什么都不发生，也不会用演示 Offer、空面试或旧草稿替用户制造事实。
- 下一轮候选：把已验证的可逆职位生命周期接入 canonical v3、完整备份与 UI；归档只整理、结束只记录结果，未来安排必须由用户明确选择保留或暂停，恢复时已过期事项绝不重新变成欠账。

---

## 迭代 18 · 改变记录之前，先看它会影响什么

### 体验证据

- 任务：把一个仍有待办和已安排面试的职位改成“未通过”、收进归档，再从另一个标签页修改同一条记录；随后尝试取回职位和恢复先前安排。
- 观察：阶段、归档、任务和面试原本是分散写入。用户看见的是一个简单下拉框，数据库却可能同时隐藏或取消多条未来安排；旧页面还可能拿过期预览覆盖另一个标签的新事实。恢复若不区分仍在未来、已经过时和暂停后被编辑的项目，也会把旧日期重新制造成新的压力。
- 影响：“结束一段经历”本来已经可能令人失落，如果按钮还悄悄处理日历、制造欠账或用“推进至未通过”描述结果，工具就在最不合适的时候替用户评价并扩大失控感。

### 迭代产品稿

- 目标：职位阶段只是事实状态，不是成绩。任何会影响未来安排的变化，都必须在写入前列出真实影响，并把保留或暂停的决定交还给用户。
- 生命周期：进行中、已结束、已归档是三个互斥空间；看板只承载进行中职位，已结束和已归档仍可查看完整资料。归档只整理，结束只记录结果，两者都不删除历史。
- 影响预览：阶段、归档与取回统一先生成数据库内可验证的 preview，逐项列出会涉及的待办和面试。需要选择时不预设答案；“保留安排”会继续显示并标注职位上下文，“随职位暂停”只暂停未来事项，不冒充用户主动取消。
- 恢复边界：只恢复仍在未来且暂停后未被编辑的事项；时间已过或后来改过的项目只列作说明，不自动复活。已结束又归档的职位可以取回资料，但不会借取回动作绕过终态规则。
- 并发与失败：确认时重新读取事实和时间边界；跨标签发生变化就返回新 preview，零写入。SQLite 已提交但页面刷新失败时进入“只重试刷新”状态，不能重复提交。阶段撤销只在原职位仍处于刚写入的进行中阶段时有效。
- 心境与文案：使用“阶段改为”“记录结果”“随职位暂停”，不使用“推进至未通过”、成功率、Offer 率、漏斗转化或结果评分。空的已结束/归档空间也是完整状态，不显示数量徽章催促整理。
- 验收标准：所有阶段/归档/取回入口只走同一 prepare→commit 流程；有影响的选择默认为空；取消后数据库与原阶段逐字不变；另一个标签更新、跨过时间边界或编辑暂停事项后旧确认必定零写；319px 弹层无横向溢出、关闭按钮 44×44、背景 inert、取消后焦点回到原阶段控件。
- 本轮不做：不批量猜测哪些旧安排应恢复，不把终态职位自动归档，不永久删除职位，不用任何比例评价求职表现。

### 开发与验证

- 改动：Career canonical schema 升级到 v3，加入职位归档/结束标记、任务与面试的 canonical `canceled` 状态、操作来源与不可抹除 lifecycle event ledger；精确 v0/v1/v2/v3 lineage 均在一个事务内迁移和复检。新增 `prepareCareerLifecycleChange` / `commitPreparedCareerLifecycleChange`，使用稳定 operation ID、事实 fingerprint、真实 Web Lock 和事务 guard。Career UI 新增进行中/已结束/已归档空间、影响决策弹层、refresh-only 恢复态、全局入口锁和安全撤销；面经与职位详情会把系统暂停明确显示为“随职位暂停”。
- 测试：Career v3 schema、备份迁移、生命周期存储和 preview 使用真实 SQLite 覆盖严格谱系、未知状态、迁移中断回滚、future/elapsed/edited、跨标签变化、锁等待跨时间边界、四向阶段、结束与归档合并恢复、隐私字段不出 preview 和事务失败零部分写。UI 契约 19/19、preview 11/11、lifecycle storage 14/14、ESLint 与 TypeScript 通过。真实 319×734 浏览器确认页面与 291px 决策弹层均无横向溢出；“Offer → 未通过”预览列出真实未来待办、两项选择均未预选、关闭 44×44，取消后阶段仍为 Offer 且焦点精确回到阶段选择器。验收只读，没有写入用户数据。
- 结果：通过并形成 `ac26dca`、`fa478bb`、`09c087c` 三个可回退检查点。职迹现在先解释一项变化会做什么，再等待用户决定；结果不再偷偷改写未来安排，也不再被包装成对人的评价。
- 下一轮候选：把待办从“点整行就完成”的二态勾选，改成可查看、可改期、可放下、可安全拾回的统一行动面板；删除红色逾期和“未完成数量”带来的债务感。

---

## 迭代 19 · 安排可以放下，也可以重新拾起

### 体验证据

- 任务：从今日、日历、未来七天、职位详情和联系人详情分别打开同一条待办；让原计划时间经过，再尝试完成、改期、取消和恢复；同时模拟另一标签页先修改记录，以及 SQLite 已写入但画面刷新失败。
- 观察：旧界面把整行点击等同于完成，并用红色“逾期”和未完成数量把时间经过解释成欠账；已完成或取消的事项没有统一详情，恢复可能直接沿用已经过去的时间。不同入口各自写 SQL，旧页面还能覆盖另一个标签的新事实；数据库已提交但刷新失败时，普通错误提示会诱导用户再次点击。
- 影响：求职本来就包含等待、改变计划和主动放下。若工具把每个旧日期都变成警告，把一次刷新故障伪装成保存失败，用户会为了清空列表而行动，甚至不再信任记录是否真的保存。

### 迭代产品稿

- 目标：待办只记录用户主动安排过的下一步，不评价执行速度。时间经过是一条原计划事实，不是失败；放下和重新拾起都必须可见、可逆且需要明确选择。
- 自然日期：按浏览器本地自然日显示“今天、明天、周六、原计划 8月18日、以后再说”，不使用按 24 小时相除得到的“逾期 N 天”。Today 只把今天真实安排的待办放进焦点；过去、未来和无日期事项留在日历，不自动搬到今天。
- 统一详情：今日列表、日历议程、移动未来七天、职位详情和联系人详情共同打开 `TaskDetailSheet`。独立 44px 完成按钮只负责完成，文字区域只负责打开详情，避免误触。详情如实展示待办、已完成、已放下或随职位暂停，以及可证明的职位和联系人上下文。
- 放下与恢复：放下前明确说明记录不会删除、也不会被当成失败，默认焦点放在“继续保留”。重新放回时没有预选项；用户必须选择“以后再说”、新的未来时间，或仅在旧时间仍有效时明确沿用，已经过去的原计划永不自动复活。
- 并发与失败：每次修改都携带读取时的 `updated_at`。跨标签事实变化时保留详情和输入、零写入，并要求查看最新状态。SQLite 已提交但刷新失败时进入不可关闭的“只重新读取”状态，重试不能再次执行写入；列表上的快速连点也只能触发一次根动作。
- 局部读取：联系人资料、互动、职位和待办分别保留 loading / ready / error；某部分读取失败时明确说明没有把它当成空记录，其他成功读取的内容仍可使用。
- 心境边界：不显示“逾期”“N 项未完成”“X 项可以重新安排”，不默认把联系人下一步标成重点。已放下和随职位暂停是两种不同事实；结束或归档职位下被明确保留的安排仍可见，并标注上下文。
- 验收标准：五个入口统一详情；任务文字不会完成事项；快速连点只有一次写；旧 token 零写并提示最新状态；写后刷新失败只能重读；恢复选择初始为空；319px 页面与详情无横向溢出，日期按钮和所有相关操作至少 44px；Tab / Shift+Tab 圈定，Escape 关闭后焦点回原任务入口。
- 本轮不做：不自动重新安排旧日期，不因优先级替用户制造紧迫感，不永久删除任务；本轮不改职位分析页、智能导入、材料版本或完整备份交互，它们分别进入后续真值切片。

### 开发与验证

- 改动：新增版本保护的 Career task repository 与统一 action facade，完成、改期、以后再说、放下和恢复均在单个 Career exclusive Web Lock 与单事务 guard 中执行；版本严格单调，防止同一毫秒的状态 ABA。Career UI 接入统一 TaskDetailSheet、自然日期分组、移动单日周视图、独立完成目标、稳定创建 ID、stale 与 refresh-only 状态；联系人下一步默认优先级改为普通。
- 测试：任务存储覆盖幂等创建、同 ID 冲突、版本 ABA、A 读 B 写、状态/职位变化、事务失败回滚和安全错误；UI 契约覆盖五入口、日期语言、连点去重、显式恢复、联系人局部失败、焦点圈定和窄屏 CSS。全仓 394/394、ESLint、TypeScript 与 vinext production build 通过。
- 浏览器：真实 `http://localhost:3000/career` 在 319×734 下确认 `clientWidth === scrollWidth === 319`；移动七日选择器每项 44×55，详情按钮均至少 44px。打开详情后焦点在“关闭待办详情”，Shift+Tab 到最后一个安全动作、Tab 回到关闭按钮；关闭后焦点精确归还原任务入口，body 滚动恢复。验收全程只读，没有修改用户数据。
- 结果：通过并形成 `5f39ea9` 与 `ee6c6b6` 两个可回退检查点。职迹现在允许一条安排被完成、调整、放下或稍后拾回，而不会把时间经过写成对人的评价。
- 下一轮候选：让智能导入和 CSV 先预览再提交，使用原文指纹、稳定操作 ID 与单事务保存，避免输入竞态、解析错位和刷新故障造成重复职位；随后把 AI 客户端发送范围在按钮旁逐字段说清。

---

## 迭代 20 · 原文先变成草稿，不直接变成事实

### 体验证据

- 任务：从一段职位原文、浏览器里明确选中的文字和一份 Saved Jobs CSV 建立职位记录；在模型遗漏来源、CSV 列数错位、存在同名职位、另一个标签页已提交，以及 SQLite 已写入但页面没收到回执时继续操作。
- 观察：旧入口会把 AI 返回的来源、链接和阶段直接当成事实，简单按逗号拆 CSV，并在写入后才刷新列表。同一行可能重复导入，错列可能把地点写进错误字段；刷新或回执失败又会让用户以为没有保存而再次点击。关闭输入框时也没有稳定的核对边界。
- 影响：求职记录会长期参与提醒、面试准备和材料关联。一次看似省事的“智能导入”如果发明平台、错放字段或重复职位，后续每个页面都会放大这条错误；而用户在不确定是否已保存时，最需要的是可核对事实，不是再点一次。

### 迭代产品稿

- 目标：任何外部内容都先成为可编辑预览，只有用户明确确认的字段才进入本机 SQLite。模型负责整理，不负责决定来源、阶段、重点程度或是否与既有职位合并。
- 三种入口：粘贴只发送用户放进文本框的内容；浏览器采集只带当前 URL 与用户明确选中的文字，不读取 Cookie、登录态、站内消息或整页正文；CSV 完全在浏览器本机解析，不经过 AI。三种方式共同使用同一套预览、同名提示和保存状态。
- 来源真值：只有用户输入的独立 URL 或采集 URL 可以推导 LinkedIn、BOSS直聘或官网；正文里仅仅提到平台不能成为来源证据。模型返回的 source、platform、URL、stage、status 与 priority 全部丢弃；未知来源写“待确认来源”，不猜测。
- 预览与确认：公司、职位、地点、来源、链接、薪资、办公方式、说明与标签逐项编辑；模型置信度只写“原文明确 / 可能需要核对 / 原文未说明”，不用百分比或评分。用户改过的字段单独标明“你已确认”，其他字段仍保持待核对语义。
- CSV 边界：支持 BOM、引号内换行和转义引号；空文件、超过 16 MiB、超过行列上限、表头污染与列数错位在任何写入前拒绝。完全相同的重复行折叠展示，不计入选择或保存；只有用户明确把它分成独立记录后才获得新操作身份。
- 保存真值：原文与完整预览都有指纹，操作 ID 在重试间稳定；提交时在一个 Career 独占锁和单个 SQLite 事务中写入职位与活动。相同操作与相同内容只返回已保存，不重复写；内容被改过或另一个标签产生冲突时零写入。
- 不确定结果：如果数据库可能已经提交但回执与复核都失败，界面锁住原文、选择和操作 ID，只允许只读核对。确认已存在的行转为“已保存在本机”，确认不存在的行才能继续原操作；未知或冲突时不重新生成、重签或重复提交。
- 心境边界：同名职位只是温和提示，不阻止用户记录不同批次，也不替用户合并。导入过程不显示成功率、置信百分比或“数据质量得分”；取消只说明这次输入未保存，不把核对工作变成欠账。
- 验收标准：AI 不能把文本里的 LinkedIn/BOSS 字样变成来源；CSV 错列即使手工改一个字段仍不可提交；折叠重复不进入数量或事务；写入回执丢失后提交次数仍为一；160 KiB AI 输入和 16 MiB CSV 在读取/请求前拒绝；319px 根页和 291px 弹层无横向溢出，三个 tab 和文件控件至少 44px，脏关闭有确认，最终关闭焦点回到“从原文添加”。
- 本轮不做：不抓取登录后招聘页面，不绕过 LinkedIn/BOSS直聘限制，不自动投递，不自动合并同名职位，也不把 PDF 简历或材料正文送给模型。

### 开发与验证

- 改动：新增可验证 Career import repository、RFC4180 CSV 解析、同步输入快照、完整 preview 签名、有界批量预检、稳定操作 ID、事务幂等提交、`commit_uncertain` 与只读 inspect。职位页改为“从原文添加”三入口弹层，加入可逆字段核对、同名范围提示、折叠重复/显式 fork、请求停止、dirty 关闭与 refresh-only 状态；书签采集器删除整页正文 fallback。
- 测试：存储层覆盖真实 canonical Career v3 SQLite、TOCTOU、CSV 多字节/错列/超限、重复行、同操作冲突、一次事务、回执丢失与只读复核；UI 契约覆盖三入口、全局阻断、mixed uncertain、文件读取前限额、请求中止、同名范围和焦点归还。全仓 455/455、ESLint、TypeScript 与 vinext 五环境 production build 通过。
- 浏览器：真实 `http://localhost:3000/career` 在 319×734 下确认根页 `clientWidth === scrollWidth === 319`、弹层 291/291；三个 tab 均 44px，CSV 是可见的 267×48 原生文件控件；ArrowRight 的焦点与选中同步。填写未保存原文后关闭会先进入确认，放弃后两个 dialog 均卸载，焦点精确回到“从原文添加”。验收全程没有保存职位。
- 结果：通过并形成 `2da8f92` 与 `2f6ae9b` 两个可回退检查点。职迹现在先把外部内容变成可核对草稿，再由用户决定什么才是自己的记录。
- 下一轮候选：让浏览器发往 Career AI 路由的请求本身也只包含界面逐项披露的字段；随后把材料附件的保存、删除、清理待重试和结果不确定状态做成同样可恢复的闭环。

---

## 后续迭代模板

### 体验证据

- 任务：
- 观察：
- 影响：

### 迭代产品稿

- 目标：
- 方案：
- 验收标准：
- 本轮不做：

### 开发与验证

- 改动：
- 测试：
- 结果：
- 下一轮候选：
